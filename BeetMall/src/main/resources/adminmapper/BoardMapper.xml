<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beetmall.sshj.admin.dao.BoardDAO">
 	<select id="noticeList" resultType="com.beetmall.sshj.admin.vo.BoardVO">
 		select infonum, infotype, infotitle, to_char(infowritedate, 'MM-DD HH:MI') from notice order by infonum desc
 	</select>
 	<insert id="boardInsert" parameterType="com.beetmall.sshj.admin.vo.BoardVO">
 		insert into board(no, subject, content, ip, userid) values(boardsq.nextval, #{subject}, #{content}, #{ip}, #{userid})
 	</insert>
 	<select id="boardSelect" resultType="com.beetmall.sshj.admin.vo.BoardVO">
 		select no, userid, subject, content, hit, writedate from board where no=#{param1}
 	</select>
 	<update id="boardUpdate" parameterType="com.beetmall.sshj.admin.vo.BoardVO">
 		update board set subject=#{subject}, content=#{content} where no=#{no} and userid=#{userid} 
 	</update>
</mapper>