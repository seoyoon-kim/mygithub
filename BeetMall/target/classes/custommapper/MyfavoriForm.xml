<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beetmall.sshj.custom.dao.MyfavoriFormDAO">
	<select id="myfavoriForm" resultType="com.beetmall.sshj.custom.vo.MyfavoriFormVO">
		select a.storenum, farmprofile, farmname, farmintro, count(farmname) as farmtotal
	    from farm a
	    join favorite b
	    on a.storenum=b.storenum
	    where b.userid=#{param1}
	    GROUP BY a.storenum, farmprofile, farmname, farmintro;
	</select>
	<select id="myfavoriFormTotal" resultType="com.beetmall.sshj.custom.vo.MyfavoriFormVO">
		select a.storenum, farmprofile, farmname, farmintro, count(farmname) as farmtotal
	    from farm a
	    join favorite b
	    on a.storenum=b.storenum
	    GROUP BY a.storenum, farmprofile, farmname, farmintro;
	</select>
</mapper>